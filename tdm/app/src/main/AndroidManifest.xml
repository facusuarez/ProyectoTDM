<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.masterlist"
    android:versionCode="1"
    android:versionName="1.0">

    <!--<uses-sdk android:targetSdkVersion="17" />-->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="info.androidhive.masterlist.activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="info.androidhive.masterlist.activities.Ranking.DatosProfesorActivity"
            android:label="@string/datos_profesor"></activity>
        <activity
            android:name="info.androidhive.masterlist.activities.Ranking.PuntuacionActivity"
            android:label="@string/datos_profesor"></activity>
        <activity
            android:name="info.androidhive.masterlist.activities.Ranking.MateriasActivity"
            android:label="@string/datos_profesor"></activity>
        <activity android:name="info.androidhive.masterlist.activities.authentication.RegisterActivity"></activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="info.androidhive.masterlist.activities.authentication.LoginActivity"
            android:exported="true">
            <intent-filter><action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <service
            android:name="info.androidhive.masterlist.authentication.AccountTypeService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator"/>
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

</manifest>
